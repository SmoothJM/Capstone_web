import { TrustUrlPipePipe } from './trust-url-pipe.pipe';

describe('TrustUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
